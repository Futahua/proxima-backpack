{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Tests read fixture bytes off disk, so they — and only they — get Node types.
    "types": ["node"]
  },
  "include": ["src", "tests"]
}
