{
  "name": "proxima-backpack",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Proxima Backpack for Papers \u2014 Elastic board, calendar and projects over an Obsidian vault",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "node tools/build-fixture-module.mjs && tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.tests.json",
    "build": "node tools/build-fixture-module.mjs && tsc -p tsconfig.build.json",
    "agent:bridge": "node tools/agent-vault-bridge.mjs",
    "agent:accept": "node tools/agent-accept.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}